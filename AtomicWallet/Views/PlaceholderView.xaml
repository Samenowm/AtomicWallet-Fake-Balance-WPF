<UserControl x:Class="AtomicWallet.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AtomicWallet.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:PlaceholderViewModel}">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="{Binding Title}" Style="{StaticResource Hero}"
                   HorizontalAlignment="Center" />
        <TextBlock Text="Screen coming soon" Style="{StaticResource Caption}"
                   HorizontalAlignment="Center" Margin="0,8,0,0" />
    </StackPanel>
</UserControl>
